  <!-- <div>
        <p>Mi nombre es {{nombre}}</p>
    </div>
    <br>
    <input type="text" ng-model="favMeat" placeholder="enter you fav meat"></input>
    <p>Your fav meat is: {{favMeat}}</p>
     
    <h3>Comentarios</h3>
    <ul>
        <li ng-repeat="com in comentarios">
            {{com.comentario}} - <strong>{{com.username}}</strong>
        </li>
    </ul>

    <br><br>
    <h3>Mis POSTS</h3>
    <button ng-click="llamarpeticion()">Buscar data</button>
    <ul>
        <li ng-repeat="po in posts">
            {{po.body}}
        </li>
    </ul>
    <h4 ng-show="posts.length < 1">NO HAY REGISTROS</h4> -->